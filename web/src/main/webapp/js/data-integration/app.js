/**
 * This file defines the top-level angular module for this app (and potentially any configuration information).
 * 
 * 
 */
// FIXME: Yes, it's dumb to have a unique file for 1 line of code. This is mostly a convenience so that it's easier to distinguish the individual pieces of the
// App. Once development settles down it may make sense to place all the code back into a single file.
(function($, angular) {
    "use strict";

    var app = angular.module('integrationApp', []);

    /* global jQuery, angular */
})(jQuery, angular);
